# =================================================================
# 🔧 CONFIGURATION ENVIRONNEMENT PERKUP BACKEND
# =================================================================
# Copier ce fichier vers .env et remplir les valeurs

# =================================================================
# 🗄️ BASE DE DONNÉES MONGODB
# =================================================================
# URL de connexion MongoDB (Atlas ou locale)
# Format MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/dbname
# Format local: mongodb://localhost:27017/perkup
MONGO_URI=mongodb://localhost:27017/perkup

# =================================================================
# 🔐 AUTHENTIFICATION JWT
# =================================================================
# Clé secrète pour signer les tokens JWT (générer une chaîne aléatoire forte)
# Exemple de génération: openssl rand -hex 64
JWT_SECRET=your-super-secret-jwt-key-here-generate-a-strong-random-string

# =================================================================
# 📧 SERVICE EMAIL AWS SES
# =================================================================
# Adresse email source vérifiée dans AWS SES
EMAIL_SOURCE=noreply@yourdomain.com

# Région AWS SES (ex: eu-west-1, us-east-1)
SES_REGION=eu-west-1

# =================================================================
# 💳 STRIPE CONFIGURATION
# =================================================================
# Clé secrète Stripe (commence par sk_test_ pour test, sk_live_ pour prod)
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here

# IDs des prix Stripe pour chaque plan d'abonnement
# Récupérer depuis le dashboard Stripe > Produits
STRIPE_PRICE_BASIC=price_1234567890_basic
STRIPE_PRICE_SUPER=price_1234567890_super
STRIPE_PRICE_PREMIUM=price_1234567890_premium

# Clé publique Stripe (pour le frontend)
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key_here

# =================================================================
# 🚀 REDIS CACHE (OPTIONNEL)
# =================================================================
# Activer/désactiver le système de cache Redis
CACHE_ENABLED=true

# Configuration Redis (local ou cloud)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# Ou utiliser une URL Redis complète (prioritaire si définie)
# REDIS_URL=redis://localhost:6379
# REDIS_URL=redis://username:password@hostname:port/database

# TTL (Time To Live) en secondes pour différents types de cache
REDIS_TTL_USER=1800          # 30 minutes
REDIS_TTL_SUBSCRIPTION=900   # 15 minutes
REDIS_TTL_PARTNERS=3600      # 1 heure
REDIS_TTL_GEO=86400         # 24 heures

# =================================================================
# 🌍 ENVIRONNEMENT
# =================================================================
# Environment: development, staging, production
NODE_ENV=development

# Origines autorisées pour CORS (séparées par des virgules)
# En development: * (toutes origines)
# En production: domaines spécifiques
ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com

# =================================================================
# 🔧 AWS CONFIGURATION (OPTIONNEL)
# =================================================================
# Les credentials AWS peuvent être configurés via:
# 1. Variables d'environnement (ci-dessous)
# 2. Fichier ~/.aws/credentials
# 3. Rôles IAM (recommandé en production)

# AWS_ACCESS_KEY_ID=your_aws_access_key
# AWS_SECRET_ACCESS_KEY=your_aws_secret_key
# AWS_REGION=eu-west-1

# =================================================================
# 📍 SERVICES GÉOLOCALISATION (OPTIONNEL)
# =================================================================
# Clés API pour services de géolocalisation si utilisés
# GOOGLE_MAPS_API_KEY=your_google_maps_api_key
# MAPBOX_ACCESS_TOKEN=your_mapbox_token

# =================================================================
# 🎯 CONFIGURATION SERVEUR
# =================================================================
# Port pour serverless-offline (développement local)
PORT=4000

# =================================================================
# 📊 MONITORING & LOGS (OPTIONNEL)
# =================================================================
# Niveau de logs: error, warn, info, debug
LOG_LEVEL=info

# Activer le tracing AWS X-Ray
# AWS_XRAY_TRACING_NAME=perkup-backend
# _X_AMZN_TRACE_ID=auto

# =================================================================
# 🔒 SÉCURITÉ
# =================================================================
# Rate limiting (requêtes par heure)
RATE_LIMIT_PER_HOUR=1000

# Limite de taille des requêtes (en bytes)
MAX_REQUEST_SIZE=10485760  # 10MB

# =================================================================
# 🧪 TESTS (OPTIONNEL)
# =================================================================
# Base de données de test séparée
# MONGO_URI_TEST=mongodb://localhost:27017/perkup_test

# =================================================================
# 📝 NOTES
# =================================================================
# - Générer des clés sécurisées pour JWT_SECRET
# - Configurer AWS SES pour l'envoi d'emails
# - Créer les produits et prix dans Stripe
# - Redis optionnel mais recommandé pour les performances
# - Vérifier les permissions AWS pour SES et autres services
